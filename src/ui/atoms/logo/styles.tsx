import styled from 'styled-components'
import { Link } from 'react-router-dom'
import { Icon } from 'antd'

export const LogoContanter = styled(Link)`
  text-transform: uppercase;
  font-weight: 900;
  position: relative;
  color: #fff;
`
export const LogoIcon = styled(Icon)`
  margin-right: 12px;
`
